const MULTIPLY = "multiply";

module.exports = { MULTIPLY };

("An action is a simple object that tells app what to do, using a required type property and sometimes extra data to describe the task in detail.");
